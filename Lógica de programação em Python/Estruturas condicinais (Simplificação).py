a = 10
b = 20

print("a é maior que b") if a > b else print("a é menor que b")
# Aqui fiz o uso de uma estrutura condicional composta, com a diferença de que eu usei uma técnica chamada operadores ternários, no Python, a estrutura é:
# (Bloco de código)
# Condição se (If)
# Condição senão (else)
# (Bloco de código)
# Pelo que eu entendi do Operador Ternário - É um jeito mais simples de escrever uma condição simples e composta, que a vantagem é escrever um código mais limpo, para o código fonte, ficar mais legível, mas só é aplicável se a estrutura condicional, não for muito extensa, nem tão complexa, caso seja, não vale a pena.
if a != b: print("A é diferente de b")
# Aqui eu sei a simplificação máxima da estrutura condicional simples, e assim fica mais legível no código fonte.