
# Vamos supor que uma empresa queria saber quanto de lucro teve em cada mês, para isso ela digita quanto de lucro ela teve em cada mês, de forma desordenada, e então o algoritmo arruma isso

x = [1, 2, 3, 4 , 5 , 6]
soma = 0.0
for meses in x:
 y = float(input(f"Quanto você gastou no  {meses}* mês: -   "))
 for despesas in range(1):
  # Range = A set of different things of the same type
  soma += y 
print("-", float(soma))
    
def sum_variable (lista)
 n = len(lista)
 # len means length, in portuguese "Largura/cardinalidade" of a list, dictionary and so on.
 for i in range(n):
  # Esse "for" ele tem uma variável contadora "i" e "in range" é uma função do python que faz a iteração sequencial de uma coleção de valores numéricos. n é o parâmetro que será passado para essa estrutura de repetição, e então o for iniciará
 



